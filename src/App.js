import React from 'react';\nimport MoodSelector from './components/MoodSelector';\nimport HeatMapCalendar from './components/HeatMapCalendar';\nimport useLocalStorage from './hooks/useLocalStorage';\nfunction App(){const [entries,setEntries]=useLocalStorage('moodEntries',[]);const addMood=m=>{const today=new Date().toISOString().slice(0,10);setEntries([...entries,{date:today,mood:m}]);};return(<div><MoodSelector onSelect={addMood}/><HeatMapCalendar data={entries}/></div>);} export default App;